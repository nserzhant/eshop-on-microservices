# CUSTOMER AUTHORIZATION SERVER RELEASE PIPELINE

resources:
  repositories:
  - repository: self
    trigger: none
  pipelines:
  - pipeline: customer-authserver-ci
    source: customer-authserver-ci
    trigger: none

variables:
  suffix: ${{ split(variables['System.TeamProjectId'], '-')[0]}} # Pseudo - random string based on project Id
  azureserviceconnection: 'azure-spn'
  webappname: 'customer-authserver-web-app-$(suffix)'
  administratorLoginPassword: '$(sqlPasswordSecret)' # Create secret using variables section
  templateFileName: 'webapp-with-sql.bicep'
  subscriptionid: '$(subscription-id)' # Create the "subscription-id" pipeline variable in Variables section or replace the expression by the value
  resourceGroupName: 'eshop'
  location: 'westeurope'
  clientAppId: '$(client-app-id)' # Optional. Create the "client-app-id" pipeline variable in Variables section or replace the expression by the value
  clientAppOrigin: '$(client-app-origin)' # Optional. Create the "client-app-origin" pipeline variable in Variables section or replace the expression by the value
pool:
  vmImage: windows-latest

steps:
- checkout: none
- download: customer-authserver-ci

- task: AzureResourceManagerTemplateDeployment@3
  displayName: Deploy Web App Bicep
  inputs:
    deploymentScope: 'Resource Group'
    azureResourceManagerConnection: '$(azureserviceconnection)'
    subscriptionId: '$(subscriptionid)'
    action: 'Create Or Update Resource Group'
    resourceGroupName: '$(resourceGroupName)'
    location: '$(location)'
    templateLocation: 'Linked artifact'
    csmFile: '$(Pipeline.Workspace)/customer-authserver-ci/Bicep/$(templateFileName)'
    overrideParameters: '-webAppName $(webappname) -administratorLoginPassword $(administratorLoginPassword) -clientAppId $(clientAppId) -clientAppOrigin $(clientAppOrigin) -location $(location) -suffix $(suffix)'
    deploymentMode: 'Incremental'

- powershell: az cache purge
  displayName: Clear Azure Cache

- task: AzureRmWebAppDeployment@4
  displayName: Deploy Web App
  inputs:
    ConnectionType: 'AzureRM'
    azureSubscription: '$(azureserviceconnection)'
    appType: 'webApp'
    WebAppName:  '$(webappname)' 
    packageForLinux: '$(Pipeline.Workspace)/**/*.zip'


- powershell: Invoke-WebRequest -UseBasicParsing https://$(webappname).azurewebsites.net/ -TimeoutSec 300
  displayName: Init And Check Web App